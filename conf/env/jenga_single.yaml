name: jenga_single
args: []
kwargs: 
  object_name: "jenga_single"

load_dir: ""

dataset:
  _target_: "datasets.jenga_dset_proc.load_jenga_slice_train_val"
  n_rollout: null 
  normalize_action: ${normalize_action}
  # data_path: "/mnt/diffusion_policy/tasks/pick_place/dual_wrist/world_model/"
  data_path: "/mnt/ssd_data/tasks/jenga_mujoco_noise"

  split_ratio: 0.9
  # These are now critical for the 3x pairing logic
  action_freq: 10
  img_freq: 30
  transform:
    _target_: "datasets.img_transforms.default_transform"
    img_size: ${img_size}

decoder_path: null
num_workers: 8  